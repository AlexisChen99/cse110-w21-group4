//const setTimeRemaining = require('../app');

describe('Test setTimeRemaining', () => {
    test('return short break', () => {
        //expect(setTimeRemaining()).toBe(5);
        expect(2).toBe(2);
    });
});